pca:
  n_components: 2
  output_path: data/artifacts/pca_projection.npy

autoencoder:
  latent_dim: 2
  batch_size: 128
  epochs: 20
  learning_rate: 0.001
  weights_path: data/artifacts/autoencoder.pt
  projection_path: data/artifacts/autoencoder_projection.npy
